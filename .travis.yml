language: haskell
install:
    - sudo apt-get install s3cmd python-imaging
    - cabal install
script: dist/build/travisblog/travisblog build
after_script:
    - dist/build/travisblog/travisblog deploy
